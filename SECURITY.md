# 安全策略

## 支持范围

项目处于个人使用和面试展示阶段，只维护默认分支最新版本。

## 漏洞报告

请通过 GitHub Security Advisory 私下报告：

- API Key 或 Token 泄露；
- MCP 未授权访问；
- SSRF、路径穿越或命令注入；
- 未审计市场数值进入 Agent 回答；
- Web 内容覆盖市场工具；
- Agent 生成无证据因果关系或交易指令。

不要在公开 Issue 中提交真实密钥、持仓、私有文档或漏洞载荷。

## 密钥

- 真实密钥只放在被 Git 忽略的本地配置或环境变量中；
- 日志不得记录完整密钥；
- 密钥一旦进入提交历史或公开输出，必须立即吊销；
- 对外部署 MCP HTTP 时必须增加 TLS、身份认证和网络访问控制。

## 金融数据安全

市场事实采用默认拒绝：

```text
AKShare
  -> Schema / 时效 / 审计
  -> ToolEnvelope
  -> LangGraph Agent FactRef 与输出校验
```

缺少有效审计的数据不能进入分析结论。模型和 Web 不能补齐净值、价格、PE、PB、收益率、
回撤、交易状态或实体存在性。

## Web 安全

Web Research MCP 必须：

- 只访问公网 HTTP/HTTPS；
- 拒绝 URL 用户凭证；
- 解析 DNS 并拒绝非公网 IP；
- 对每次重定向重新校验；
- 限制响应大小、内容类型、正文长度和重定向次数；
- 可配置域名 allowlist；
- 固定 `numeric_allowed=false`；
- 将抓取文本视为不可信数据而非指令。

## Agent 安全

LangGraph Agent 必须：

- 使用固定 `StateGraph`，不允许模型动态增加节点、边或工具；
- 使用固定工具白名单；
- 不执行任意 Shell、SQL 或 URL；
- 工具结果必须先经过 `VALIDATE_TOOL_ENVELOPES` 节点才能进入关联说明；
- 回答中的市场数字逐项匹配工具信封；
- 关联说明引用事实字段；
- 禁止把相关性写成确定性因果；
- 工具失败时回答“当前无法确认”；
- 不生成确定性交易指令。

## 非目标

项目不执行交易、不托管券商账户、不承诺收益，也不替代持牌投资顾问。
