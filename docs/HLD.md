# 金融数据分析 Agent 高层设计

## 1. 目的

本项目面向两个目标：

1. **面试展示**：展示 MCP 工具化、金融数据审计、错误语义、确定性计算和受控 Agent
   等工程能力。
2. **个人使用**：提供中国基金、指数、ETF 和 A 股的数据分析与关联说明，辅助研究，
   不替用户决策。

当前功能优先级是基金和个股的单标数据分析。组合分析、策略回测、持仓管理和生产级
平台暂不进入当前范围。

## 2. 产品定位

项目是“**可信数据工具 + LangGraph 受控研究 Agent**”，不是：

- 自动交易系统；
- 收益预测或荐股系统；
- 大而全的金融终端；
- 多 Agent 自由协作平台；
- 量化因子、组合优化或回测平台。

Agent 的价值不在于产生新的金融事实，而在于：

- 理解用户研究问题；
- 选择已注册的 MCP 工具；
- 将基金、指数、个股和外部背景结果按语义关联；
- 区分事实、历史统计、关联关系和限制；
- 用可追溯的方式解释结果。

## 3. 核心原则

### 3.1 市场事实只能来自工具

以下内容只能来自通过 Schema、时效和 `frame_sha256` 审计的 AKShare 结果：

- 基金净值、ETF/股票价格、指数点位；
- PE、PB、收益率、波动率、回撤和历史分位；
- 申购、赎回、限额和交易状态；
- 基金、股票和指数是否存在及其规范代码。

模型不得生成、补齐、插值、前向填充、修复或改写这些内容。

### 3.2 计算必须确定性

收益、波动、回撤、历史分位、ETF 溢价等由 Skill 的固定公式计算。Agent 只能解释
已经计算好的结果，不能在自然语言中重算。

### 3.3 关联不等于因果

Agent 可以说明两个已审计事实同时出现或可能相关，但必须使用：

- “同时存在”；
- “历史上表现为”；
- “可能与……有关，需要更多证据确认”；
- “不能据此证明因果关系”。

禁止使用“因为 A，所以 B 必然发生”这类无证据因果表达。

### 3.4 失败语义必须明确

必须区分：

- `NOT_FOUND`：目录查询成功但无实体；
- `AMBIGUOUS`：有多个候选，需要用户确认；
- `UNSUPPORTED`：实体可能存在，但当前接口不支持；
- `UPSTREAM_ERROR`：数据源失败，无法判断；
- `STALE_DATA`：数据存在但时效不满足要求。

上游失败时只能回答“当前无法确认”，不能回答“该标的不存在”。

## 4. 当前实现状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| AKShare Skill | 已实现 | 实体解析、接口调用、数据校验、确定性指标和审计 |
| Fund Advisor MCP | 已实现 | 九个强类型市场事实工具 |
| Web Research MCP | 已实现 | 三个外部背景工具 |
| LangGraph Agent | 已实现 | 固定状态图、工具路由、FactRef、关联说明和输出校验 |
| API / Web UI | 当前不做 | 面试和个人分析阶段不需要 |
| PostgreSQL / Redis / Elasticsearch | 当前不做 | 不引入与当前目标无关的平台复杂度 |
| 组合分析 / 回测 | 后续扩展 | 单标分析稳定后再评估 |

MCP 命名空间、依赖、入口、配置和测试接线已完成，本地 stdio 工具发现与真实 Agent
指数估值闭环已验证。Compose 已简化为两个 MCP 服务，runtime/test 镜像、容器内测试、
服务健康检查、HTTP 工具发现和 Agent → MCP → Skill 容器闭环均已验证。Ark thinking
模型的 Pydantic 结构化关联输出与响应门禁也已通过真实配置验证。

## 5. 目标架构

```text
用户问题
   |
   v
LangGraph Agent Host
   |-- 固定 StateGraph
   |-- 问题分类
   |-- 工具白名单与调用计划
   |-- 结果关联
   |-- 事实/关联/限制分层表达
   `-- 输出校验
   |
   +----------------------+----------------------+
   |                                             |
   v                                             v
Fund Advisor MCP                           Web Research MCP
九个市场事实工具                           三个外部背景工具
   |                                             |
   v                                             v
AKShare Skill                               公网搜索/页面/文档
审计事实与确定性指标                         numeric_allowed=false
```

### 5.1 Fund Advisor MCP

| 工具 | 作用 |
| --- | --- |
| `fund_search` | 搜索基金候选，不自动选择份额 |
| `fund_status` | 查询申购、赎回或场内交易状态 |
| `fund_analyze` | 分析基金历史收益、风险和持有体验 |
| `fund_profile` | 查询类型、经理、费用、规模和资产配置 |
| `fund_rating` | 按代码查询第三方评级 |
| `index_valuation` | 指数 PE TTM / PB 历史分位 |
| `stock_valuation` | A 股 PE TTM / PB / 前复权价格历史 |
| `fund_compare` | 比较两到五只基金并提示口径差异 |
| `interface_audit` | 审计真实接口和 Schema |

MCP 负责参数契约、超时、缓存和统一 `ToolEnvelope`，不得修改 Skill 数值。

### 5.2 Web Research MCP

| 工具 | 作用 |
| --- | --- |
| `web_search` | 搜索非数值背景 |
| `web_fetch` | 安全读取网页正文 |
| `document_read` | 按用户 URL 读取 HTML、文本或 PDF |

Web 结果固定 `numeric_allowed=false`。它可以补充政策、公告或文档背景，但不能确认
市场实体，也不能提供或覆盖市场数值。

启用 Web Research 后，基金和股票单标分析固定追加“研究/媒体文章”和“博主/社区观点”
两类可选搜索。来源类别由域名和标题规则确定，仅用于结果分组，不代表账号身份或内容
真实性已经核验。可选搜索失败时保留市场分析并标记 `partial_result`；市场工具失败时
不得用 Web 结果替代。

### 5.3 LangGraph Agent

Agent 使用 LangGraph `StateGraph` 实现固定、有向、可测试的状态图。选择 LangGraph 是为了
显式表达节点、条件边和终止状态，不是为了增加模型自治能力。

第一版固定闭环：

```text
QUESTION
  -> CLASSIFY
  -> PLAN_REGISTERED_TOOLS
  -> CALL_MCP
  -> VALIDATE_TOOL_ENVELOPES
  -> BUILD_ASSOCIATIONS
  -> VALIDATE_RESPONSE
  -> RENDER_ANSWER
  -> ANSWER
```

条件边只允许进入以下终止分支：

```text
AMBIGUOUS       -> need_clarification
NOT_FOUND       -> not_found
UNSUPPORTED     -> unsupported
UPSTREAM_ERROR  -> cannot_confirm
STALE_DATA      -> stale_data
PARTIAL_RESULT  -> partial_result
```

边界：

- 图结构、工具白名单、错误映射和放行条件由代码定义；
- 模型只参与受限意图补充和关联说明，不决定市场事实是否有效；
- Agent 只能通过 MCP Client 调用工具，不直接 import `FundAdvisor`；
- 第一版不配置数据库 checkpoint、长期会话或跨会话 Memory；
- 不使用 LangChain AgentExecutor、开放式 ReAct、动态工具规划、多 Agent 或自治反思循环；
- 图保持无环；失败后由用户重新发起，不在图内无限重试。

## 6. 基金与个股数据分析

### 6.1 基金分析

基金分析按以下顺序组织：

1. **实体与产品**：代码、名称、类型、经理、基准、费用、规模。
2. **数据口径**：场外累计净值、ETF/LOF 历史价格或货币基金收益率。
3. **历史表现**：区间收益、年化波动、下行波动、最大回撤和修复时间。
4. **持有体验**：最长水下期、月度收益统计、滚动观察期收益分布。
5. **底层估值**：仅当跟踪指数可精确解析时解释指数 PE/PB。
6. **交易因素**：申赎状态、场内价格、IOPV 和溢价。
7. **限制**：数据日期、样本长度、接口缺失和不可比项。

基金净值历史位置不是估值。主动基金不得因为净值处于历史低位就被描述为“低估”。

### 6.2 指数分析

PE TTM 和 PB 分别计算、分别解释：

- PE 反映相对盈利的历史估值位置；
- PB 反映相对净资产的历史估值位置；
- 两者口径不同，不平均、不合成“综合估值分”；
- 历史分位只说明历史位置，不预测收益。

### 6.3 个股分析

个股分析包含：

- PE TTM 当前值与历史分位；
- PB 当前值与历史分位；
- 前复权价格历史；
- 数据可用性、观察区间和时效。

价格、PE 和 PB 使用独立序列。价格上涨与估值分位变化可以关联说明，但不能直接写成
盈利改善、资金推动等因果结论，除非有额外工具证据。

### 6.4 比较分析

比较前先检查：

- 基金类型；
- A/C 类等份额类别；
- 净值、价格或收益率口径；
- 跟踪指数或业绩基准；
- 观察区间和数据日期。

不可比时输出差异，不计算总分。

## 7. LangGraph Agent 关联说明设计

### 7.1 输入

关联说明只能消费成功的 `ToolEnvelope`，并保留：

- 工具名和请求参数；
- 规范实体；
- 指标、单位和日期；
- 数据审计；
- 数据警告和错误。

### 7.2 输出结构

```text
事实
  - 原样展示工具事实

关联说明
  - 描述多个事实如何共同影响研究理解
  - 标明这是关联，不是因果

限制
  - 数据日期、缺失接口、不可比口径和历史统计局限

参考结论
  - 条件式表达，不给确定性交易指令
```

### 7.3 允许的关联

- 产品类型与历史风险特征；
- 基金与精确跟踪指数的估值关系；
- ETF 溢价与场内执行风险；
- 个股价格历史位置与 PE/PB 历史位置；
- 多只基金的收益、风险、费用和基准差异；
- 数据质量对结论可信度的影响。

### 7.4 禁止的关联

- 用网页文本覆盖市场工具；
- 用名称猜测基金跟踪指数；
- 把净值低位写成估值低位；
- 把同时发生写成因果关系；
- 从历史分位推导未来收益概率；
- 用缺失字段生成综合评分；
- 给出“必买、必卖、稳赚、抄底”等指令。

## 8. 面试工程亮点

项目应重点展示：

1. **MCP 标准化边界**：工具发现、Schema、stdio/HTTP 传输和 Client/Server 分工。
2. **金融数据契约**：实体解析、Schema、时效、`frame_sha256` 和字段口径。
3. **错误模型**：不存在、歧义、不支持、过期和上游失败分离。
4. **确定性计算**：模型不参与收益、回撤、分位和溢价计算。
5. **数据源隔离**：市场事实 MCP 与 Web 背景 MCP 分离。
6. **上游治理**：多搜索供应商降级、AKShare 接口降级、SSRF 防护。
7. **可测试性**：固定夹具验证公式与错误语义，真实 `audit` 验证上游。
8. **LangGraph 受控编排**：固定节点、条件边、工具白名单和响应校验，不追求自由自治。

## 9. 实施顺序

### 阶段 0：MCP 接线（已完成）

- 修复包命名空间、依赖、配置、入口、imports 和 tests；
- 删除已移除 Agent/API/数据库留下的配置与部署项；
- 验证九个市场事实工具和三个 Web 工具；
- 第一版只使用进程内缓存，不部署 Redis 服务；
- 保留 Skill 审计字段。

### 阶段 1：单标数据分析（已完成）

- 已整理基金、指数和个股输出结构；
- 已补齐分组口径、数据日期、审计引用和 warning；
- 已合并可用评级，并增加受可比性门禁约束的比较排序；
- 保持 Skill CLI 稳定。

### 阶段 2：最小 LangGraph Agent（核心已完成）

- 建立 Pydantic `AgentState` 和固定 `StateGraph`；
- 实现规则优先的问题分类和代码工具白名单；
- 通过 MCP Client 接入九个市场工具和三个 Web 工具，单标分析按固定计划并发调用一个
  市场工具和两类可选 Web 搜索；
- 生成带事实引用的字段关联说明；
- 实现事实、关联、限制的确定性渲染；
- 校验无来源数字、Web 数值和无证据因果表达。

### 阶段 3：基金与股票候选筛选（接口审计已完成）

- 已盘点并真实审计 24 个财务、行业和基金质量候选接口；
- 已明确股票横截面、财务深挖、同分类行业比较和基金候选池的数据链路；
- 已拒绝当前契约破损、语义不清或作用域不符的接口；
- `stock_screen`、`fund_screen` Skill/MCP/Agent 能力尚未实现；
- 第一版只输出规则通过、失败和未知，不生成综合 AI 分数。

### 后续

- 组合分析；
- 规则回测；
- 提醒和长期持仓；
- API、UI 和持久化。

后续能力只有在确有个人使用价值时才引入，不为展示技术栈而增加组件。
