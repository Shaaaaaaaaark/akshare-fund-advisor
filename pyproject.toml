[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "akshare-fund-advisor"
version = "0.1.0"
description = "Audited LangGraph research agent for Chinese funds and stocks"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "akshare-fund-advisor contributors"}]
dependencies = [
    "akshare==1.18.64",
    "pandas==2.3.3",
    "numpy==2.0.2",
    "requests==2.32.5",
    "curl_cffi==0.13.0",
    "pydantic==2.13.4",
    "PyYAML==6.0.3",
    "langgraph==1.2.10",
    "mcp==1.29.0",
    "openai==2.53.0",
    "httpx==0.28.1",
    "beautifulsoup4==4.15.0",
    "pypdf==6.14.2",
]

[project.scripts]
fund-advisor-mcp = "fund_advisor_mcp.fund.server:main"
web-research-mcp = "fund_advisor_mcp.web.server:main"
fund-advisor-agent = "fund_advisor_agent.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["fund_advisor_mcp*", "fund_advisor_agent*"]

[tool.pytest.ini_options]
testpaths = ["tests", "skills/akshare-fund-advisor/tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["build"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]
